# CareOps AI — Final Audit (Qdrant & Data Hygiene)

**Date:** 2026-08-05  
**Branch:** `careops-ai`  
**Auditor:** Automated setup + manual verification  

---

## 1. Qdrant health

| Check | Result |
|-------|--------|
| `GET /healthz` | ✅ `healthz check passed` |
| Endpoint | `http://localhost:6333` |

---

## 2. Seeding scripts executed

From `apps/api` with virtualenv activated:

```bash
source venv/bin/activate
python ../../scripts/seed_qdrant_memory.py
python ../../scripts/seed_hospital_policies.py
```

| Script | Result |
|--------|--------|
| `seed_qdrant_memory.py` | ✅ Completed (~5.5 min, 42 hospital SOPs + 175 complaint vectors) |
| `seed_hospital_policies.py` | ✅ Completed (10 indexed policies) |

---

## 3. Qdrant collection counts (post-seed)

| Collection | Points | Contents |
|------------|--------|----------|
| `sop_memory` | **52** | 42 operational SOPs + 10 hospital policies (`POLICY-001` … `POLICY-010`) |
| `complaint_memory` | **175** | Hospital patient feedback from Postgres (synthetic) |
| `planning_memory` | **1** | Legacy planning memory (no restaurant keywords) |

**Restaurant / Swiggy vector purge:** ✅ **0 hits** in all three collections after reseed (keyword scan for pizza, garlic bread, mozzarella, swiggy, restaurant, etc.).

---

## 4. Synthetic data attestation

| Data source | Synthetic? | Evidence |
|-------------|------------|----------|
| Patient feedback (`feedback` table) | ✅ Yes | 175 entries; **0** restaurant/pizza keywords; hospital ED/OPD/ICU language |
| Safety incidents (`safety_incidents`) | ✅ Yes | 3 rows prefixed `"Synthetic incident:"` / `"Synthetic feedback:"` |
| FHIR fixtures (`data/fhir/`) | ✅ Yes | IDs `SYN-PAT-001` … `SYN-PAT-005`; no names, MRNs, or addresses |
| Encounters / appointments | ✅ Yes | Generated by `seed_demo_data.py` + `seed_hospital_data.py`; no real PHI |

See `data/fhir/README.md` for privacy policy.

---

## 5. Idempotency verification

| Script | Method | Result |
|--------|--------|--------|
| `seed_qdrant_memory.py` | Purges all `org_id=1` vectors from `sop_memory` + `complaint_memory` before insert | ✅ Deterministic reseed (purge-then-insert) |
| `seed_hospital_policies.py` | Purges `domain=hospital_operations` policy vectors before insert | ✅ Ran **twice** consecutively → `sop_memory` stayed **52** (no duplicate growth) |

Added helpers in `MemoryService`:
- `purge_org_memory(org_id)`
- `purge_policy_vectors(org_id)`
- `purge_legacy_restaurant_vectors(collections)`

---

## 6. Policy RAG spot-check (3 golden questions)

Source: `apps/api/tests/fixtures/careops_golden_policy_questions.json`

| Question | Expected policy | Top citation | Pass |
|----------|-----------------|--------------|------|
| ED surge escalation threshold for occupancy? | `POLICY-001` | "Emergency surge: when ED occupancy exceeds 85%…" | ✅ |
| Do critical PPE reorders require human approval? | `POLICY-004` | "Supply reorder: critical PPE and respiratory supplies require human approval…" | ✅ |
| Does CareOps provide medication or treatment advice? | `POLICY-009` | "CareOps disclaimer: … never diagnosis, treatment, or medication advice." | ✅ |

All three returned valid `citations[]` with matching `policy_id` and excerpt text.

---

## 7. RAGAS / DeepEval eval results

```bash
pytest evals/test_ragas_complaint.py evals/test_deepeval_quality.py -v -W ignore::DeprecationWarning
```

| Suite | Tests | Result | Duration |
|-------|-------|--------|----------|
| RAGAS (`test_ragas_complaint.py`) | 2 | ✅ PASSED (faithfulness ≥ 0.8, context precision reported) | — |
| DeepEval (`test_deepeval_quality.py`) | 6 | ✅ PASSED (hallucination + relevancy) | — |
| **Total** | **8/8** | ✅ **All passed** | 154 s |

> **Note:** RAGAS/DeepEval fixtures are still static hand-written samples (`evals/complaint_rag_samples.json`). They gate component quality; live Qdrant content is hospital-native after this reseed.

---

## 8. Secrets / `.env` tracking

| Check | Result |
|-------|--------|
| Tracked `.env` files | ✅ **None** — only `apps/api/.env.example` is tracked |
| Tracked secret/credential files | ✅ **None found** |
| Local `.env` | Present at `apps/api/.env` (gitignored, not committed) |

---

## 9. Git remote

```
personal  https://github.com/shubhangini67/careops.git
```

Published branch: `careops-ai` (single-author history, shubhangini67 only).

---

## 10. Recommended next steps

1. Set `careops-ai` as default branch on GitHub if needed
2. Optional: promote RAGAS/DeepEval fixtures from hospital complaint samples (`scripts/build_ragas_dataset.py`)

---

*CareOps AI — Qdrant reseed audit.*
